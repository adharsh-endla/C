WEEK 3:
a. Caesar cipher (shift cipher)
Algorithm / concept: Encryption & Decryption in Java for:
a. Caesar cipher (shift cipher)
Algorithm: Caesar Cipher
•	Encryption: cipher = (plaintext + shift) % 26
•	Decryption: plaintext = (cipher - shift + 26) % 26
Example
Plaintext: HELLO
Shift: 3
Encrypted: KHOOR
Decrypted: HELLO

a. Caesar cipher (shift cipher)
Program
import java.util.Scanner;
public class CaesarCipher {

    // Encrypt the text using Caesar Cipher
    public static String encrypt(String text, int shift) {
        StringBuilder result = new StringBuilder();
        for (char character : text.toCharArray()) {
            if (Character.isUpperCase(character)) {
                char ch = (char)(((character - 'A' + shift) % 26) + 'A');
                result.append(ch);
            } else if (Character.isLowerCase(character)) {
                char ch = (char)(((character - 'a' + shift) % 26) + 'a');
                result.append(ch);
            } else {
                // Non-alphabet characters remain the same
                result.append(character);
            }
        }
        return result.toString();
    }
    // Decrypt the text using Caesar Cipher
    public static String decrypt(String text, int shift) {
        // Decryption is encryption with (26 - shift)
        return encrypt(text, 26 - (shift % 26));
    }

    // Main method to test Caesar Cipher
    public static void main(String[] args) {
        Scanner scanner = new Scanner(System.in);
        System.out.print("Enter text to encrypt: ");
        String input = scanner.nextLine();
        System.out.print("Enter shift key (0-25): ");
        int shift = scanner.nextInt();
        String encrypted = encrypt(input, shift);
        String decrypted = decrypt(encrypted, shift);
        System.out.println("\nEncrypted Text: " + encrypted);
        System.out.println("Decrypted Text: " + decrypted);

    }
}
        System.out.println("\nEncrypted Text: " + encrypted);
        System.out.println("Decrypted Text: " + decrypted);
    }
}


Output:  
                                                          
gedit CaesarCipher.java
vaagdevi@vaagdevi-OptiPlex-7060:~$ javac CaesarCipher.java
vaagdevi@vaagdevi-OptiPlex-7060:~$ java CaesarCipher
Enter text to encrypt: hello world
Enter shift key (0-25): 3

Encrypted Text: khoor zruog
Decrypted Text: hello world



